// config.js
// Centralized backend URL for your frontend to talk to Railway backend.

const CONFIG = {
  // ✅ Your live Railway backend (no trailing slash)
  BACKEND_URL: "https://veo-backend-production.up.railway.app"
};

export default CONFIG;
